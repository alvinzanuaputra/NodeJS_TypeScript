# Example environment variables for Next.js (frontend)

# Base URL for API requests
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Feature flags (optional)
NEXT_PUBLIC_FEATURE_EXPERIMENTAL=true

# Analytics (jika digunakan)
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Vercel environment (optional, ditentukan otomatis saat deploy)
# VERCEL_ENV=development

# Custom secrets (do NOT prefix with NEXT_PUBLIC_ if it's only for server-side usage)
SECRET_KEY=your-secret-key

# Database connection string (if applicable)
# DATABASE_URL=postgres://user:password@localhost:5432/mydatabase
# Authentication settings (if applicable)
# AUTH0_DOMAIN=your-auth0-domain
# AUTH0_CLIENT_ID=your-auth0-client-id
# Custom environment variables for your application
# CUSTOM_VAR=your-custom-value
# Note: Do not commit this file to version control if it contains sensitive information.
# Use .env.local.example as a template for your .env.local file
# Uncomment and set the values as needed.
# Example for local development



            